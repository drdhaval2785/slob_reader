name: slob_reader
description: A pure Dart implementation of the Slob (Sorted List of Blobs) file format reader.
version: 0.1.2
repository: https://github.com/itkach/slob
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  archive: ^4.0.9

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
